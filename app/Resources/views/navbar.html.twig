{% block NAVIGATION_BAR %}
 {% set foo = app.request.attributes.get('_route') %}
 {#{{ foo }}#}
{% set foo = foo|split('_') %}
{#{{ foo[0] }} #}

    {#        
  <div class="navbar-inverse navbar-static-top">
        <div class="navbar-inner">
        <div class="container">#}


    <div class="navbar navbar-fixed-top navbar-inverse">
        <div class="navbar-inner">
            <div class="container-fluid"> 
                          {#  <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse"> 
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span> 
                                <span class="icon-bar"></span> 
                            </a>
                        #}
                    <a class="brand" href="#">
                        <i class="icon-white icon-home"></i>
                                {% block toto %}I-CHANGE(v1.2a){% endblock %}</a>
 

                    <div class="btn-group pull-right"> 
                        <a class="btn dropdown-toggle" data-toggle="dropdown" href="#"> <i class="icon-user"></i>
                                        {% if is_granted("ROLE_USER") %}
                                        {{ app.user.username }} 
                                     {% else %}
                                Username
                                    {% endif %}
                                <span class="caret"></span> 
                            </a>


                            <ul class="dropdown-menu">
                                <!--  <li class="divider"></li>-->

                                       {% if is_granted("ROLE_ADMIN") %}
                                <li><a href="{{ path('sonata_admin_dashboard') }}">{{ 'Admin Dashboard' }}</a>

                                       {% endif %}   

                                <li><a href="{{ path('sonata_user_profile_edit_authentication') }}">{{ 'Edit Profile' }}</a>
                                <li><a href="{{ path('fos_user_registration_register') }}">{{ 'Register'|trans }}</a>
                                </li>

                                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                                <li><a href="{{ path('fos_user_security_logout') }}">Déconnexion</a></li>
                                <li><a href="{{ path('fos_user_change_password') }}">{{ 'Changer password' }}</a></li>
                                    {% else %}
                                <li>  <a href="{{ path('fos_user_security_login') }}">Connexion</a></li>
                                    {% endif %}   
                            </ul>
                        </div>
                        <div class="nav-collapse collapse">
                            <ul class="nav">

           {% if foo[0] == 'changements' %}
                                <li class="dropdown active">
                                    {% else %}  
                                    <li class="dropdown">
                                    {% endif %}
                                            <a class="dropdown-toggle" data-toggle="dropdown" href="#"><i class="icon-white  icon-th"></i>
                                                Changements<b class="caret"></b> </a>
                                            <ul class="dropdown-menu">
                                                <li><a href="{{ path('changements_post') }}"><i class="icon-camera"></i> {{ 'Changements'|trans }}</a></li>
                                                  <li><a href="{{ path('changements_charts') }}"><i class="icon-camera"></i> {{ 'Graphiques'|trans }}</a></li>
                                                <li><a href="{{ path('changements_calendar') }}"><i class="icon-time"></i> {{ 'Calendrier'|trans }}</a></li>
                                                <li><a href="{{ path('changements_apy') }}"><i class="icon-camera"></i> {{ 'Changements DataGrid'|trans }}</a></li>
                                                <li><a href="{{ path('docchangements') }}"><i class="icon-camera"></i> {{ 'Fichiers'|trans }}</a></li>
    


                                            </ul>
                                        </li>
                                                                          
                                       
                                   {% if foo[0] == 'certificatscenter' %}
                                                                <li class="dropdown active">
                                    {% else %}  
                                                                    <li class="dropdown">
                                    {% endif %}
                                                                            <a class="dropdown-toggle" data-toggle="dropdown" href="#"><i class="icon-white  icon-fire"></i>
                                                                                Certificats<b class="caret"></b> </a>
                                                                            <ul class="dropdown-menu">
                                                                                <li><a href="{{ path('certificatscenter') }}"><i class="icon-fire"></i> {{ 'Certificats'|trans }}</a></li>
                                                                                <li><a href="{{ path('certificatscenter_viewapy') }}"><i class="icon-plus-sign"></i> {{ 'Certificats-APY'|trans }}</a></li>
                                                             {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                                                                                <li><a href="{{ path('certificatscenter_new') }}"><i class="icon-camera"></i> {{ 'Nouveau Certificat'|trans }}</a></li>
                                                                                <li><a href="{{ path('certificatscenter_check') }}"><i class="icon-camera"></i> {{ 'Vérifier Certificat'|trans }}</a></li>
                                                            {% endif %}
                                                                            </ul>
                                                                        </li>

   {% if is_granted("ROLE_SUPERADMIN") %}

                                                                        <li class="dropdown"> <a class="dropdown-toggle" data-toggle="dropdown" href="#"><i class="icon-white icon-wrench"></i>
                                                                                Administration<b class="caret"></b> </a>
                                                                            <ul class="dropdown-menu">
                                                                                <li><a href="{{ path('chronouser') }}"><i class="icon-camera"></i> {{ 'Utilisateurs'|trans }}</a></li>
                                                                                <li><a href="{{ path('chronogroup') }}"><i class="icon-plus-sign"></i> {{ 'Groupes'|trans }}</a></li>
                                                                                <li><a href="{{ path('environnement') }}"><i class="icon-camera"></i> {{ 'Environnements'|trans }}</a></li>
                                                                                 <li><a href="{{ path('users') }}"><i class="icon-camera"></i> {{ 'Eservices-Users(FOSUSER)'|trans }}</a></li>                            
                                                                                <li><a href="{{ path('applications') }}"><i class="icon-camera"></i> {{ 'Applications'|trans }}</a></li>
                                                                                <li><a href="{{ path('projets') }}"><i class="icon-camera"></i> {{ 'Projets'|trans }}</a></li>


                                                                            </ul>
                                                                        </li>
{% endif %}

                                                                    </ul>  

                                                                </div>
                                                                <!--/.nav-collapse -->
                            {#  <form class="navbar-search pull-left">
    <input type="text" class="search-query" placeholder="Search">
    </form>#}
                                                            </div>
                                                        </div> 

                                                    </div>
{% endblock %}    