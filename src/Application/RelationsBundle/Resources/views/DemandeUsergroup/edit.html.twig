{% extends "ApplicationRelationsBundle::layout.html.twig" %}

  {% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('bundles/applicationrelations/css/sliding.css') }}" type="text/css" media="all"/>

 {% endblock %}

{% block content_header '' %}
{% block title %}EDITION DEMANDE DE GROUPE{% endblock %}
{% block content %}


{% for type, flashMessages in app.session.flashbag.all() %}
    {% for flashMessage in flashMessages %}
<div class="alert alert-{{ type }}">
            {{ flashMessage|trans }}
    </div>
    {% endfor %}
{% endfor %}

 <a href="{{ path('groupedemande') }}" class="btn btn-large btn-success">Retour</a>

    <form action="{{ path('groupedemande_edit', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
      {#  <input type="hidden" name="_method" value="PUT" />#}
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit" class="btn btn-large btn-warning">Edit</button>
        </p>
    </form>

     
        <form action="{{ path('groupedemande_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit" class="btn btn-large btn-error">Delete</button>
        </form>

{% endblock %}
