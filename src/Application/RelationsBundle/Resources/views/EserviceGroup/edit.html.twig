{% extends "ApplicationEservicesBundle::layout.html.twig" %}

  {% block stylesheets %}
{{ parent() }}
      <link rel="stylesheet" href="{{ asset('bundles/applicationeservices/css/sliding.css') }}" type="text/css" media="all"/>
       <style>
         .single {
         background-color: #f1f1f1;
        }
        </style>
 {% endblock %}
 

{% block content_header '' %}
{% block title %}
EDITION GROUPE
{% endblock %}
{% block content %}

    
<br><br>
                
                                       

    <form action="{{ path('egroup_groupes_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
         <input type="hidden" name="_method" value="PUT" />
         <table class="table table-bordered">
    <tbody>         <tr><td>
                 <div>
                   {{ form_widget(edit_form) }}
                {#     {{ form_row(form.services) }}#}
              </div>
         
               </td></tr>
                                                
<tr><td colspan="3">
                                   
  <button type="submit" class="btn btn-small btn-warning">Modifier</button>
     </form>                                   
                                 
               </td></tr>
           
   </tbody></table>  

           
                       

  
    <a href="{{ path('egroup_groupes')  }}" class="btn btn-small btn-success">Retour</a>
  
         
         
         
   {# <form action="{{ path('egroup_groupes_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>
#}
    {#    <form action="{{ path('egroup_groupes_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
#}

{% endblock %}
