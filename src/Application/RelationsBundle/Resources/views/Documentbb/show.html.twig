{% extends "ApplicationRelationsBundle::layout.html.twig" %}

  {% block stylesheets %}
{{ parent() }}
      <link rel="stylesheet" href="{{ asset('bundles/applicationrelations/css/sliding.css') }}" type="text/css" media="all"/>
       <link rel="stylesheet" href="{{ asset('bundles/applicationrelations/css/tableview.css') }}" type="text/css" media="all"/>
 {% endblock %}

{% block content_header '' %}
{% block title %}DETAILS DOCUMENT:  {{ entity.name }} {% endblock %}
{% block content %}
      
     <br><br>
     
<table class="view table-bordered">

 <thead>
                        <tr class="title">
                            <th>Paramètres</th>
                            <th>Valeurs</th>
                        </tr>
                    </thead>
    <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>md5</th>
                <td>{{ entity.md5 }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ entity.path }}</td>
            </tr>
             <tr>
                <th>Projet</th>
                <td>{{ entity.idprojet|join('<br> ')|raw }}</td>
            </tr>
            
               <tr>
            <td colspan="2" class="calendar"> 
 
         <form action="{{ path('projets_documents_delete', { 'id': entity.id }) }}" method="post">
          <input type="hidden" name="_method" value="DELETE" />
        {{ form_errors(delete_form) }}
           {{ form_row(delete_form.id) }}
           {{ form_rest(delete_form) }}
                                        <div style="text-align:right">
                                            <button type="submit" class="btn btn-small btn-danger" onclick="if(!confirm('Are you sure?')) { return false; }">Supprimer Document</button>
                                        </div>
                                    </form>
  
                                </td>
                            </tr>
        </tbody>
    </table>

      <a href="{{ path('projets_documents') }}" class="btn btn-large btn-success">Retour</a>      <ul class="record_actions">
   
   
        
{% endblock %}
