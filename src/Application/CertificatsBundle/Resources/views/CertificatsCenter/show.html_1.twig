{% extends "ApplicationCertificatsBundle::layout.html.twig" %}

  {% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('bundles/whiteoctoberpagerfanta/css/pagerfantaDefault.css') }}" type="text/css" media="all" />
{% endblock %}


{% block title %}Certificat Détails{% endblock %}


{% block content_header '' %}

{% block content %}
{% for type, flashMessages in app.session.flashbag.all() %}
    {% for flashMessage in flashMessages %}
        <div class="alert alert-{{ type }}">
            {{ flashMessage|trans }}
        </div>
    {% endfor %}
{% endfor %}
     <a href="{{ path(btnretour) }}" class="btn btn-small btn-success">Retour</a>
      {% if is_granted("ROLE_ADMIN") %}           
<a href="{{ path('certificatscenter_edit', { 'id': entity.id }) }}" class="btn btn-small btn-warning">Editer</a>
{% endif %}

<br> <br> 


<table class="table table-striped table-bordered table-hover">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Filename</th>
            <td>{{ entity.fileName }}</td>
        </tr>
        <tr>
            <th>Project</th>
               <td>{{ entity.project.nomprojet }}</td>
          
        </tr>
        <tr>
            <th>Typecert</th>
            <td>{{ entity.typeCert.FileType }}</td>
        </tr>
        <tr>
            <th>Cnname</th>
            <td>{{ entity.cnName }}</td>
        </tr>
        <tr>
            <th>Startdate</th>
            <td>{{ entity.startDate|date('Y-m-d') }}</td>
        </tr>
        <tr class="error">
                    <!--<td>{{ entity.endTime|date('Y-m-d H:i:s') }}</td>-->
            <th class="warning">Endtime</th>
            <td>{{ entity.endTime|date('Y-m-d') }}</td>
        </tr>
        <tr>
            <th>Date d'Ajout</th>
            <td>{{ entity.addedDate|date('Y-m-d H:i:s') }}</td>
        </tr>
        
        <tr>
            <th>Date de modification</th>
            <td>{{ entity.updatedDate|date('Y-m-d H:i:s') }}</td>
        </tr>
        
        <tr>
            <th>Servername</th>
            <td>{{ entity.serverName }}</td>
        </tr>
        <tr>
            <th>Port</th>
            <td>{{ entity.port }}</td>
        </tr>
        <tr>
            <th>Servicename</th>
            <td>{{ entity.serviceName }}</td>
        </tr>
        <tr>
            <th>Way</th>
            <td>{{ entity.way }}</td>
        </tr>
        <tr>
            <th>Statusfile</th>
            <td>{{ entity.statusFile }}</td>
        </tr>
        
  
 <tr><th>Applications</th><td>
             {% for appli in applis %} {{ appli.nomapplis }}

       {% endfor %}
                        </td>
        </tr>
    </tbody>
</table>




{% endblock %}